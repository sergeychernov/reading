{
	"functions": {
		"api/index.ts": {
			"maxDuration": 60
		}
	},
	"rewrites": [
		{
			"source": "/(.*)",
			"destination": "/api"
		}
	],
	"buildCommand": "yarn workspace @reading/epub-utils build && yarn workspace @reading/llm-schemas build && yarn workspace @reading/data build && yarn workspace @reading/jobs build"
}
